## 1. Fundação: pipeline e layouts base

> Nota de sequenciamento (ajuste sobre a ordem original do design.md — "Migration Plan"): remover React/Inertia de `package.json`/`composer.json` e do `vite.config.js` só é seguro depois que NENHUM controller chama mais `Inertia::render` (fim da fase 4). Enquanto isso, o novo entry point JS puro convive com o antigo `app.jsx` (dois entries no mesmo `vite.config.js`, plugin React ainda presente). 1.1/1.2 abaixo cobrem só a adição do novo entry; a remoção efetiva das dependências virou 5.1/5.3.

- [x] 1.1 Adicionar o novo entry point JS puro (`resources/js/app.js`) ao `vite.config.js`, mantendo `resources/js/app.jsx` e o plugin React intactos até a fase 5 (remoção de dependências fica em 5.1/5.3).
- [x] 1.2 Ajustar `vite.config.js`: adicionar `resources/js/app.js` aos `input` do plugin `laravel-vite-plugin`, ao lado de `resources/css/app.css` e `resources/js/app.jsx` (que segue existindo até a fase 5).
- [x] 1.3 Criar `resources/views/components/layouts/app.blade.php` (base HTML para as novas views Blade, com `@vite(['resources/css/app.css', 'resources/js/app.js'])`, sem `@inertia`/`@inertiaHead`/`@viteReactRefresh`) mantendo `@include('partials.theme-script')`, favicon, fontes; `resources/views/app.blade.php` (raiz Inertia) não é tocado até a fase 5. Nota: ficou em `components/layouts/` (não `layouts/`) para que `<x-layouts.app>` resolva via a convenção de auto-discovery de componentes do Blade — os outros layouts (public/internal/auth) o consomem assim.
- [x] 1.4 Criar `resources/views/components/layouts/public.blade.php` a partir de `Layouts/PublicLayout.jsx` (header, footer, container, flash messages, alternância de tema, aviso de e-mail não verificado/perfil incompleto lendo `auth('candidato')->user()` direto — sem prop `auth.candidato` vinda de Inertia).
- [x] 1.5 Criar `resources/views/components/layouts/internal.blade.php` + partial `partials/internal-sidebar.blade.php` a partir de `Layouts/InternalLayout.jsx` (sidebar coordenador/gestor, topbar, slots `pageTitle`/`breadcrumb`/`topbarActions`, menu mobile via off-canvas).
- [x] 1.6 Criar `resources/views/components/layouts/auth.blade.php` a partir de `Layouts/AuthLayout.jsx`.
- [x] 1.7 Criar componentes Blade de UI genéricos sem estado em `resources/views/components/ui/` (button, input, textarea, label, badge, alert(+title/description), card(+header/title/description/content/footer), table(+header/body/row/head/cell), separator, avatar(+fallback), progress, input-group(+addon)) traduzindo classes Tailwind 1:1 dos equivalentes em `resources/js/components/ui/*.jsx`. `select`/`dialog`/`sheet`/`dropdown-menu`/`popover`/`command`/`tabs`/`switch`/`checkbox`/`radio-group`/`alert-dialog` ficam para quando as páginas que os usam forem migradas (fases 2-4), priorizando HTML nativo por decisão de design.
- [x] 1.8 Criar componentes Blade compostos em `resources/views/components/`: `logo`, `flash-messages` (substitui `FlashMessages.jsx` + `sonner`, toasts via `resources/js/flash.js`), `empty-state`, `stat-card`, `field` (wired ao `$errors` bag do Laravel em vez de prop de erro manual), `pagination` (usa `linkCollection()` do paginator Laravel — mesmo formato de link que o JSON antigo), `badges/*` (status-vaga, status-candidatura, tipo, tipo-admissao, modalidade, nova — reaproveitando `Vaga::$statusLabel`/`$tiposLabel`/`$modalidadesLabel` e `Candidatura::$statusLabel` já existentes no PHP, em vez de duplicar em `lib/enums.js`), `politica-privacidade-conteudo`, `icons/whats-app`.
- [x] 1.9 Implementar `resources/js/tema.js` (liga botões `[data-tema-toggle]` ao `window.toggleTheme()` já definido em `partials/theme-script.blade.php`) e `resources/js/app.js` como entry point que inicializa todos os widgets em `DOMContentLoaded` e expõe `window.initWidgets()` para re-inicializar fragmentos trocados por fetch.
- [x] 1.10 Implementar `resources/js/senha-forca.js` (medidor de força, substitui `PasswordStrengthMeter.jsx`) como widget reutilizável por `data-senha-forca-input`.
- [x] 1.11 Implementar `resources/js/curriculo-dropzone.js` (arrastar/soltar, preview, limpar; substitui `CurriculoDropzone.jsx`) como widget por `data-curriculo-dropzone`, com fallback sem JS (input nativo visível). Também implementado `resources/js/ui/sheet.js` (off-canvas genérico usado pelo menu mobile dos layouts) e `resources/js/flash.js`, não previstos originalmente mas necessários para os layouts 1.4/1.5.
- [x] 1.12 Ajuste de escopo: `lib/format.js` e `lib/enums.js` NÃO foram portados para JS — como o Blade renderiza no servidor, os rótulos/formatações desses módulos viram lógica PHP (accessors/estáticos já existentes em `Vaga`/`Candidatura`, mais o novo helper `app/Support/Iniciais.php` para as iniciais do avatar). `lib/cpf.js` (máscara de CPF/CEP/telefone digitada pelo usuário) continua necessária em JS e fica adiada para a fase 3 (formulários de candidato), onde é de fato usada.

## 2. Público: home, listagem/detalhe de vagas, inscrição, alertas

- [x] 2.1 Migrar `Publico/Vagas/Index.jsx` para `resources/views/publico/vagas/index.blade.php`: hero + busca, filtros (tipo, escolaridade, projeto, cidade/UF, faixa salarial, ordenação), lista + painel de detalhe master-detail, paginação, estados vazio/indisponível. Verificado visualmente (Playwright) em desktop (1600px) e mobile (390px).
- [x] 2.2 Implementar o comportamento "filtro auto-aplica" em JS puro (`resources/js/vagas-filtro.js`): campos vivem fora do `<form id="filtro-vagas">` no DOM (associados via atributo `form=""`, já que estão espalhados entre o hero e a barra lateral) — `change` num select dispara fetch para a mesma rota GET com `X-Requested-With`, troca só `[data-vagas-resultado]` e atualiza a URL via `history.pushState`; sem JS, os campos ainda submetem o formulário GET normalmente. Verificado: troca de `tipo` filtra de 3 para 2 vagas sem reload, URL vira `?tipo=U&ordenar=recentes`.
- [x] 2.3 Implementar `x-vaga-detalhe` (era `VagaDetalhePainel.jsx`) e `x-vaga-item` (era `VagaListaItem.jsx`) como componentes Blade, reaproveitados na renderização cheia e no fragmento `_resultado.blade.php` usado tanto pela página completa quanto pela resposta AJAX.
- [x] 2.4 Implementar a versão mobile do painel de detalhe como off-canvas em JS puro (`resources/js/ui/sheet.js`, genérico, reaproveitado pelo menu mobile dos layouts): ao clicar um item da lista abaixo de xl, `resources/js/vagas-selecao.js` clona o painel de detalhe (já no DOM, oculto) para dentro do off-canvas e abre. Sem fetch adicional — mesma garantia do original.
- [x] 2.5 Ajustar `VagaPublicaController` (`index`/`listagemIndisponivel`) para responder com `publico.vagas.index` completo ou só `publico.vagas._resultado` quando `$request->ajax()` (header `X-Requested-With`).
- [x] 2.6 Migrar `Publico/Vagas/Show.jsx` para `resources/views/publico/vagas/show.blade.php`, incluindo copiar link (`resources/js/copiar-link.js`) e compartilhar no WhatsApp.
- [x] 2.7 Migrar `Publico/Candidatura.jsx` → `resources/views/publico/candidatura.blade.php` (dados de conferência, `select`/checkbox nativos no lugar de Radix, honeypot, toggle do campo de detalhe do conflito de interesse em `resources/js/candidatura.js`) e `Publico/Confirmacao.jsx` → `resources/views/publico/confirmacao.blade.php`. Rótulos de nível de escolaridade que só existiam em `lib/enums.js` viraram `CandidatoFormacao::$niveisLabel`.
- [x] 2.8 Migrar `Publico/Alertas.jsx` → `resources/views/publico/alertas.blade.php` (checkboxes nativos `name="areas[]"` etc., sem JS necessário) e `Publico/AlertaCancelado.jsx` → `resources/views/publico/alerta-cancelado.blade.php`. **`Publico/ConsultaCandidatura.jsx` não foi migrada**: não há rota `candidatura.consulta.busca` nem controller que a renderize — `routes/web.php` só tem `candidatura.consulta` como redirect legado para `/minha-conta/candidaturas` (comentário no próprio arquivo confirma). É código morto, apagado na 5.1 junto com o resto do `resources/js/Pages`.
- [x] 2.9 Migrar `Publico/PoliticaPrivacidade.jsx` (usa `x-politica-privacidade-conteudo`, já criado na 1.8) e `Publico/FazendaRessacada.jsx` (página estática, sem props); trocado `Route::inertia(...)` por `Route::view(...)` em `routes/web.php`.
- [x] 2.10 Migrado `Pages/Error.jsx` para `resources/views/errors/{403,404,419,500,503}.blade.php` (convenção nativa do Laravel) com um componente `x-erro-pagina` compartilhado. `bootstrap/app.php` tinha um `$exceptions->respond(...)` que reescrevia essas respostas para `Inertia::render('Error', ...)`; ajustado para só interceptar quando a requisição é de fato uma visita Inertia (`X-Inertia` presente — páginas ainda não migradas), senão cai no padrão do Laravel.
- [x] 2.11 Atualizado `VagaPublicaController`, `InscricaoController`, `AlertaVagaController` trocando `Inertia::render` por `view()`.
- [ ] 2.12 Ajuste de sequenciamento (mesmo motivo do item 1 sobre `package.json`): os helpers Inertia de `tests/TestCase.php` continuam usados por testes de páginas ainda não migradas (fases 3-4) — removê-los agora quebraria esses testes. Cada teste passa a usar `assertViewIs`/`assertSee`/`assertDontSee`/`assertViewHas` nativos do Laravel conforme sua página é migrada; a remoção efetiva dos helpers de `TestCase.php` (quando nenhum teste mais os chamar) vira parte da 5.1.
- [x] 2.13 Atualizado `tests/Feature/VagaPublicaTest.php`, `tests/Feature/InscricaoTest.php`, `tests/Feature/AlertaVagaTest.php` para as novas asserções Blade. **`tests/Feature/Drhflow/AcompanhamentoInscricaoTest.php` NÃO é desta fase** (engano no plano original): ele exercita `/minha-conta/candidaturas*`, isto é, `MinhaCandidaturaController`/`Candidato/Candidaturas/*.jsx` — movido para a fase 3 (item 3.5, abaixo).

## 3. Candidato: conta, perfil, minhas candidaturas

- [x] 3.1 Migrar `Candidato/Auth/{Login,Registro,EsqueciSenha,RedefinirSenha,VerificarEmail}.jsx` para views em `resources/views/candidato/auth/`. `Registro` reimplementa a checagem de CPF ao vivo (`resources/js/cadastro-cpf.js`, porta de `lib/cpf.js`), o toggle mostrar/ocultar senha (`resources/js/senha-visivel.js`) e o modal de Política de Privacidade como `<dialog>` nativo (`resources/js/ui/dialog.js`) em vez de Radix Dialog.
- [x] 3.2 Migrar `Candidato/Perfil/Edit.jsx` (dados, formação repetível, acessibilidade, endereço com autopreenchimento por CEP, preferências, currículo, senha, excluir conta). Novos widgets: `resources/js/formacoes.js` (adicionar/remover linhas de formação clonando um `<template>` renderizado a partir do próprio partial Blade `_formacao.blade.php`), `resources/js/campo-condicional.js` (mostrar/esconder genérico por valor de outro campo — substitui os vários `{condicao && <Field>}` do React), `resources/js/cep-autofill.js`, `resources/js/telefone-mask.js`. `AlertDialog` (remover currículo, excluir conta) virou `<dialog>` nativo com formulário de confirmação dentro. **Simplificação deliberada**: a barra de completude do perfil deixou de recalcular ao vivo a cada tecla (fazia sentido em SPA com estado no cliente); agora reflete o estado salvo (`Candidato::estadoCompletude()`), atualizado a cada submit — o gate de candidatura já era sempre do servidor, então não há perda de garantia, só uma pequena perda de imediatismo visual.
- [x] 3.3 Migrar `Candidato/Candidaturas/Index.jsx` e `Show.jsx` (com `AndamentoInscricao.jsx` → `x-andamento-inscricao`/`x-andamento-badge`). Correção ao plano original: `CandidaturaTimeline.jsx` não é usado por nenhuma página do candidato (o próprio comentário do código-fonte confirma: "segue existindo para o coordenador, sobre as vagas do caminho legado") — fica para a fase 4.
- [x] 3.4 Atualizar `CandidatoLoginController`, `CandidatoRegistroController`, `CandidatoRecuperarSenhaController`, `CandidatoVerificacaoController`, `PerfilController`, `MinhaCandidaturaController` trocando `Inertia::render` por `view()`.
- [x] 3.5 Atualizado `tests/Feature/Drhflow/AcompanhamentoInscricaoTest.php` para as novas asserções Blade (todas as 9 passam). `CadastroMinimoTest.php` e `CandidatoRegistroCpfTest.php` re-rodados sem edição, continuam verdes.
- [ ] 3.5 Ajuste de escopo (engano no plano original): `CandidaturaTriagemTest.php` exercita `/coord/*` (fase 4) e `AuthTest.php` exercita o login interno `Auth/Login.jsx` (também fase 4) — nenhum dos dois é desta fase. `ExclusaoContaTest.php` também NÃO é desta fase apesar do nome: sua única asserção Inertia (`acessoExpirado`) exercita `/coord/vagas/.../candidaturas/...`, isto é, o coordenador vendo uma candidatura de conta excluída — vai para a 4.5. O teste de fato desta fase é `tests/Feature/Drhflow/AcompanhamentoInscricaoTest.php` (movido da fase 2 — exercita `/minha-conta/candidaturas*`). `CadastroMinimoTest.php` e `CandidatoRegistroCpfTest.php` não usam helpers Inertia; re-rodar para confirmar que continuam verdes, sem editar.

## 4. Coordenador e Gestor: painéis internos

- [ ] 4.1 Migrar `Coord/Dashboard.jsx`, `Coord/Vagas/{Index,Form}.jsx`, `Coord/Candidaturas/{Index,Show,Todas}.jsx` para `resources/views/coord/**`, usando o layout interno (1.5).
- [ ] 4.2 Migrar `Gestor/Dashboard.jsx`, `Gestor/Vagas/{Index,Show}.jsx` para `resources/views/gestor/**`.
- [ ] 4.3 Migrar componentes usados só no painel interno: `candidaturas-list.jsx`, `VagaCard.jsx`, `CandidaturaTimeline.jsx` (status próprio das vagas do caminho legado — não confundir com `x-andamento-inscricao`, que é o andamento derivado do DRHFlow usado pelo candidato), `StatCard.jsx` (se ainda não coberto em 1.8).
- [ ] 4.4 Atualizar `VagaController`, `CandidaturaController`, `DashboardController` trocando `Inertia::render` por `view()`.
- [ ] 4.5 Atualizar `tests/Feature/VagaCoordenadorTest.php`, `tests/Feature/VagaGestorTest.php`, `tests/Feature/CandidaturaTriagemTest.php`, `tests/Feature/AuthTest.php` (login interno) e `tests/Feature/ExclusaoContaTest.php` (uma asserção sobre `/coord/vagas/.../candidaturas/...`) para as novas asserções Blade; rodar e confirmar verde.

## 5. Limpeza final

- [ ] 5.1 Apagar `resources/js/Pages`, `resources/js/Components`, `resources/js/components` (a cópia duplicada por case), `resources/js/Layouts`, `resources/js/hooks`, `resources/js/app.jsx`, `resources/js/bootstrap.js` (se específico de Inertia/React) e qualquer `.jsx` remanescente.
- [ ] 5.2 Revisar `app/Providers/AppServiceProvider.php`: decidir se `Vite::prefetch(concurrency: 3)` ainda se aplica ao novo pipeline JS puro (era otimização de bundle SPA) e ajustar/remover.
- [ ] 5.3 Rodar `composer install`/`npm install` limpos e `npm run build` para confirmar que o pipeline novo compila sem dependências React/Inertia residuais.
- [ ] 5.4 Rodar a suíte completa (`php artisan test` / phpunit) e confirmar 100% verde.
- [ ] 5.5 Validar manualmente no navegador (via skill `run`) os fluxos principais: home/listagem de vagas com filtros, inscrição com upload de currículo, cadastro/login/recuperação de senha de candidato, perfil do candidato, painel do coordenador (vagas + candidaturas), painel do gestor (autorizar vaga), alternância de tema.
- [ ] 5.6 Atualizar `README.md` se ele documentar o stack React/Inertia.
