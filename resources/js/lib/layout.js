/* Largura das páginas públicas largas (listagem de vagas e alertas) e da navbar do PublicLayout.
   10-80-10: 10% de margem de cada lado com 80% de conteúdo, a partir de lg.
   Abaixo disso o percentual daria margens inúteis — vale largura cheia com padding.
   O rodapé do PublicLayout segue em max-w-6xl e não usa esta largura. */
export const CONTAINER_LARGO = 'mx-auto w-full px-4 lg:w-4/5 lg:px-0';
